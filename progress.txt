# Progress Log

## Learnings

- EDATE DAX function was not in unconvertible list, causing invalid MDX to be generated
- AtScale MDX does not have an EDATE equivalent for measure expressions
- DAX uses "N/A" strings as null indicators; MDX uses NULL - need to transform comparisons

---

## Session: Fix N/A String-as-Null Pattern Type Mismatch

**Initial Error:**
```
IfFunction requires both results to be of the same type:
[ConstantValue[StringType](N/A): StringType] ,
[SwitchConditional(...): IntType]
```

**Root cause:** DAX pattern `IF([Measure] = "N/A", "N/A", numeric_expr)` produces MDX with type mismatch. The IFERROR template converts `"N/A"` fallback to NULL, but downstream measures still compare against `"N/A"` string.

**Fix needed:** Transform conditions like `[Measure] = "N/A"` to `ISEMPTY([Measure])` in IF/SWITCH templates.

---

## Iteration 1 - US-001: Add transformNullPlaceholderComparison helper method
- Added `transformNullPlaceholderComparison(conditionMdx: string)` method to IfTemplate
- Handles both orderings: `<expr> = "N/A"` and `"N/A" = <expr>`
- Uses regex to match all NULL_PLACEHOLDER_STRINGS (N/A, n/a, NA, na, -, "")
- Transforms matched patterns to `ISEMPTY(<expr>)`
- Files changed: if-template.ts, PRD.md
- Learnings:
  - NULL_PLACEHOLDER_STRINGS set was already defined and reusable
  - Regex needs to escape special chars when building from the set
  - Method is public to allow use from convert() in next US
---

## Iteration 2 - US-002: Transform N/A comparisons to ISEMPTY in IfTemplate
- Integrated `transformNullPlaceholderComparison()` into `convert()` method
- After converting condition via `convertSubExpression()`, now calls transform method
- Changed `conditionMdx` from `const` to `let` to allow reassignment
- Files changed: if-template.ts, PRD.md
- Learnings:
  - Simple 3-line integration - just call the helper after conversion
  - Existing null-placeholder-to-NULL logic for true/false branches still works
  - Tests pass with 63% conversion rate (unchanged from before)
---

## Iteration 3 - US-003: Validate mol.bim.json conversion and deploy
- Ran conversion - `Total margin with SC - change compared to plan (%)` now uses `ISEMPTY()` correctly
- Deploy failed with NEW error on `CAPEX utilisation - measures`
- New error: `IfFunction requires both results to be of the same type: [ConstantValue[StringType](CAPEX UTILISATION): StringType] , [SwitchConditional(...): DoubleType]`
- Root cause: DAX SWITCH returns BOTH string labels (e.g. "CAPEX UTILISATION") AND numeric measures in different cases
- This is a fundamentally different pattern - not N/A-as-null, but mixed-type UI labels
- Original DAX: `SWITCH([Selector], 1, "CAPEX UTILISATION", 2, [NumericMeasure], 3, "N/A", ...)`
- The converter correctly turns "N/A" → NULL but leaves label strings as strings
- Learnings:
  - DAX supports mixed-type SWITCH returns (strings AND numbers)
  - MDX/AtScale requires type consistency across all branches
  - Need follow-up story to handle UI label strings in SWITCH statements
---

## Iteration 4 - US-004: Handle mixed-type SWITCH with UI label strings
- Added `isStringLiteral()` method to SwitchTemplate
- Updated type normalization to convert ANY string literal to NULL in numeric context
- Changed from `isNullPlaceholderString` to `isStringLiteral` for type normalization
- Files changed: switch-template.ts
- Result: CAPEX utilisation error FIXED - now uses NULL instead of "CAPEX UTILISATION"
- Deploy hit NEW error: `Constructor function not defined at value` on `Number of special transactions for PL interim - actual`
- Root cause: DAX `VALUE()` function incorrectly converted - MDX doesn't have VALUE equivalent
- Learnings:
  - MDX requires type consistency; converting ALL string literals to NULL in numeric SWITCH works
  - New issue: DAX VALUE() function needs to be added to unconvertible list or handled specially
---

## Iteration 5 - US-005: Add VALUE function to unconvertible DAX functions
- Added VALUE to unconvertible list in function-mappings.json
- Files changed: function-mappings.json
- Result: VALUE error FIXED - measure now falls back to TODO stub
- Deploy hit NEW error: `'CloseParen' expected but Comma found` on `Write-off in selling price - average per ses wo filter`
- Root cause: CALCULATE with ALL(Table[Column]) produces malformed MDX tuple syntax
- Generated MDX: `([dimension].[Hierarchy].[All], (division))` - comma is wrong
- Learnings:
  - function-mappings.json is the central place to mark functions as unconvertible
  - The CALCULATE with ALL filter context conversion has a bug in tuple syntax
---

## Iteration 6 - US-006: Reject CALCULATE with ALL() filters
- Modified calculate-template to reject ALL() filters (AtScale MDX doesn't support tuple syntax in calculations)
- Conversion rate dropped from 63% to 57% (expected - safer fallback to TODO)
- Files changed: calculate-template.ts
- Result: Tuple syntax error FIXED
- Deploy hit NEW error: `Measure SUPPLIER_ID in calculation is not a measure`
- Root cause: Fact table column in CALCULATE filter incorrectly referenced as [Measures].[SUPPLIER_ID]
- DAX: `CALCULATE(SUM(fact[col]), fact[SUPPLIER_ID] > 0)` - filter on fact table column
- MDX has no direct equivalent for fact table column filters (row-level filtering vs. aggregated data)
- Learnings:
  - ALL() filter removal is safest to reject until proper scoping is implemented
  - Fact table columns in CALCULATE filters need special handling - cannot simply use [Measures].[X]
---
