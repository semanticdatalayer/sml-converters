# Progress Log

## Learnings (from previous sessions)

- EDATE DAX function was not in unconvertible list, causing invalid MDX to be generated
- AtScale MDX does not have an EDATE equivalent for measure expressions
- DAX uses "N/A" strings as null indicators; MDX uses NULL - need to transform comparisons
- AtScale MDX is stricter than standard MDX - test deployments early
- DAX filter context modifications (ALL, column filters) don't translate to MDX
- Type consistency is critical - MDX IIF requires same type in both branches
- Fallback to TODO stub is safer than producing invalid MDX
- DAX BLANK() in nested expressions needs toMdx() handling → NULL
- DAX & operator for string concat → MDX + operator
- BIM measures with sourceColumn but no DAX expression need to be created as base metrics when referenced
- NOW() and TODAY() in DAX often used with date arithmetic - no MDX equivalent, must produce TODO stub
- BIM source files can have bugs (references to non-existent columns/measures) - converter should detect and produce TODO stubs
- Time intelligence conversions (SAMEPERIODLASTYEAR, etc.) depend on dimensions existing - must validate before generating MDX
- resolveDimensionHierarchy() should return undefined when table doesn't exist to trigger TODO fallback

---

## Iteration 1 - US-001: Pre-populate measTables for correct table classification
- **Problem**: Tables with BIM measures but on "right" side of relationships were incorrectly classified as dimension-only tables
- **Root cause**: `populateTableLists()` classified tables before measure conversion ran, so `measTables` was empty
- **Fix**: Added `prePopulateMeasTables()` method to scan BIM tables for measures BEFORE table classification
- **Files changed**:
  - `src/commands/bim-to-sml/bim-converter/table-converter.ts` - Added `prePopulateMeasTables()` method
  - `src/commands/bim-to-sml/bim-converter/bim-to-sml-converter.ts` - Call `prePopulateMeasTables()` before `populateTableLists()`
- **Learnings for future iterations**:
  - Table classification depends on `measTables` to know which tables have measures
  - Order of operations matters: table classification must run AFTER measTables is populated
  - Tables can be in BOTH factTables and dimTables (if they have measures AND are on "right" side)
  - The `isDimensionOnlyTable()` check returns true only if table is in dimTables but NOT in factTables
- **Verified**:
  - FactPolicyLine_bim.json now shows "Fact Policy Line" in factTables
  - Marketing_-_Advertising_bim.json conversion works
  - RetailPolicyLine_bim.json conversion works
  - npm run test-custom-calcs passes
  - Typecheck passes
---

## Iteration 2 - US-001: Fix deployment errors for three test files
- **Problem**: FactPolicyLine, RetailPolicyLine, and Marketing_-_Advertising failed to deploy
- **Root causes**:
  1. `NOW()` DAX function with date arithmetic produces invalid MDX (timestamp - number fails)
  2. Column/measure references to non-existent objects fell back to invalid MDX
  3. Time intelligence functions (SAMEPERIODLASTYEAR) for non-existent tables created invalid dimension references
- **Fixes**:
  1. Added NOW and TODAY to unconvertible list in function-mappings.json
  2. Changed ColumnReference.toMdx() to throw error for unresolved references instead of fallback
  3. Changed TableColumnReference.toMdx() to throw when column doesn't exist
  4. Updated resolveDimensionHierarchy() to return undefined when table doesn't exist
- **Files changed**:
  - `src/commands/bim-to-sml/bim-converter/conversion-templates/function-mappings.json` - Added NOW, TODAY to unconvertible
  - `src/commands/bim-to-sml/bim-converter/dax-converter.ts` - Fixed fallback to throw errors
  - `src/commands/bim-to-sml/bim-converter/converter-utils.ts` - Updated resolveDimensionHierarchy signature
- **Learnings for future iterations**:
  - NOW() in DAX commonly used with date arithmetic - no MDX equivalent, must produce TODO stub
  - Source BIM files can have bugs (referencing non-existent columns/measures) - converter should detect and produce TODO stubs
  - Time intelligence functions depend on dimensions existing - must validate before generating MDX
  - "Failing safe" with TODO stubs is better than generating invalid MDX
- **Verified**:
  - FactPolicyLine_bim.json deploys successfully
  - RetailPolicyLine_bim.json deploys successfully
  - Marketing_-_Advertising_bim.json deploys successfully
  - npm run test-custom-calcs passes
  - Typecheck passes
---

## NEW PRD: Fix BIM Conversion Deployment Errors (tests1 Directory)

**Started:** 2026-01-31

This PRD addresses errors in `/Users/dianne/Downloads/bim/tests1/` files as listed in `errors_to_address.txt`.

### Error Categories

| Category | Files | Error Message |
|----------|-------|---------------|
| Non-existing metric | FactPolicyLine, Marketing_-_Advertising, RetailPolicyLine | "expression contains a non-existing metric" |
| Array undefined | Assembly_kpis, EU_Safety_Model, epm_mtd | "Cannot read properties of undefined (reading 'map')" |
| Calc not a measure | PFM_bim | "Measure ProjectID in calculation is not a measure" (separate PRD) |
| Special char parsing | Dealer_Performance_Dashboard, Global_Report, HDNA_Magellan_Report, Most_Loved | "Comma expected but OpenParen found", etc. |
| Type mismatches | FactCoverage_Pivot, FactProduction, Machining_Performance | "Function Minus requires NumericType", "IIF requires same type" |
| End of input | Magellan_-_Usage_Metrics_Report, Planogram_Integration_DataModel | "end of input expected" |

### User Stories
- US-001: Create base metrics for referenced measures
- US-002: Re-verify array handling for tests1 files
- US-003: Fix special character parsing errors
- US-004: Fix data type mismatches
- US-005: Fix "end of input expected" errors
- US-006: Add automated test-deploy script
- US-007: Test all files and document results

---

## Iteration 3 - US-002: Re-verify array handling for tests1 files
- **Problem**: Verify "Cannot read properties of undefined (reading 'map')" errors don't occur during conversion
- **Tested files**: Assembly_KPI_bim.json, Assembly_KPIs_Model_bim.json, EU_Safety_Model_bim.json, epm_mtd_bim.json
- **Result**: All four files convert successfully without undefined errors in converter code
- **Deployment blockers (AtScale server-side issues)**:
  - Assembly_KPI & Assembly_KPIs_Model: Models have no dimensions because relationships point to excluded LocalDateTable tables. Server returns "Cannot read properties of undefined (reading 'map')" for dimension-less models.
  - EU_Safety_Model & epm_mtd: Single dataset models trigger "parsedXml.schema.data-sets.data-set.map is not a function" - server-side bug with single-element arrays.
- **Files changed**: None (no converter bugs found)
- **Learnings for future iterations**:
  - BIM files with relationships only to LocalDateTable tables produce dimension-less SML models
  - AtScale server has bugs with dimension-less models and single-dataset models
  - Conversion success ≠ deployment success - server-side issues can still occur
  - "Cannot read properties of undefined (reading 'map')" can be server-side, not just converter-side
- **Verified**:
  - All four files convert without "Cannot read properties of undefined" converter errors
  - npm run test-custom-calcs passes
  - Typecheck passes
  - Deployment blockers documented as AtScale server-side issues
---
